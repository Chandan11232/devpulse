{
  "name": "devpulse-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install && curl -fsSL https://raw.githubusercontent.com/withcoral/coral/main/install.sh | sh",
    "start": "export PATH=$PATH:$HOME/.local/bin && node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@sentry/node": "^10.55.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2"
  }
}
